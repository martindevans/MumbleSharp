To run the unit tests, run the following command line from withing the MubleSharpTest folder:

`dotnet test`

Otherwise to run the test from within Visual Studio's test explorer you need at least VS2019 v16.4